import { expect } from 'chai';
import decodeToObjByStr from '../../source/utils/decode-to-obj-by-str';

describe.skip('decodeToObjByStr', () => {
  describe('解析格式', () => {
    it('',() => {
      expect(false).to.be.true;
    });
  });
});
