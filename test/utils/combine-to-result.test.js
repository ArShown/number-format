import { expect } from 'chai';
import combineToResult from '../../source/utils/combine-to-result';

describe.skip('combineToResult', () => {
  describe('組合結果', () => {
    it('',() => {
      expect(false).to.be.true;
    });
  });
});
